version: 0.2

env:
  shell: bash
  variables:
    TF_IN_AUTOMATION: "true"

phases:
  install:
    commands:
      - echo "Installing Terraform"
      - curl -sSL -o /tmp/terraform.zip https://releases.hashicorp.com/terraform/1.13.5/terraform_1.13.5_linux_amd64.zip
      - unzip -o /tmp/terraform.zip -d /usr/local/bin
      - terraform version
      - echo "Installing kubectl"
      - curl -sSL -o /usr/local/bin/kubectl https://dl.k8s.io/release/v1.30.2/bin/linux/amd64/kubectl
      - chmod +x /usr/local/bin/kubectl
      - kubectl version --client
      - echo "Installing Helm"
      - curl -sSL https://get.helm.sh/helm-v3.18.2-linux-amd64.tar.gz -o /tmp/helm.tar.gz
      - tar -xzf /tmp/helm.tar.gz -C /tmp
      - mv /tmp/linux-amd64/helm /usr/local/bin/helm
      - chmod +x /usr/local/bin/helm
      - helm version
      - echo "Installing eksctl"
      - curl -sSL https://github.com/eksctl-io/eksctl/releases/latest/download/eksctl_Linux_amd64.tar.gz -o /tmp/eksctl.tar.gz
      - tar -xzf /tmp/eksctl.tar.gz -C /tmp
      - mv /tmp/eksctl /usr/local/bin/eksctl
      - chmod +x /usr/local/bin/eksctl
      - eksctl version

  build:
    commands:
      - echo "Running Terraform for dev"
      - cd terraform/envs/dev
      - terraform init -reconfigure
      - terraform fmt -check
      - terraform validate
      - terraform plan -var-file=terraform.tfvars -var="aws_profile=" -out=tfplan
      - terraform apply -auto-approve tfplan
      - echo "Updating kubeconfig for dev-eks"
      - aws eks update-kubeconfig --region "$AWS_DEFAULT_REGION" --name "dev-eks"
      - |
        if ! kubectl get namespace argocd >/dev/null 2>&1; then
          echo "Installing ArgoCD"
          kubectl create namespace argocd --dry-run=client -o yaml | kubectl apply -f -
          kubectl apply --server-side -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml
        else
          echo "argocd namespace already exists; skipping ArgoCD bootstrap"
        fi
      - echo "Preparing AWS Load Balancer Controller IAM policy"
      - ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
      - POLICY_NAME=AWSLoadBalancerControllerIAMPolicy
      - POLICY_ARN="arn:aws:iam::${ACCOUNT_ID}:policy/${POLICY_NAME}"
      - |
        if ! aws iam get-policy --policy-arn "$POLICY_ARN" >/dev/null 2>&1; then
          echo "Creating AWS Load Balancer Controller IAM policy"
          curl -sSL -o /tmp/aws-lbc-iam-policy.json https://raw.githubusercontent.com/kubernetes-sigs/aws-load-balancer-controller/v2.14.1/docs/install/iam_policy.json
          aws iam create-policy --policy-name "$POLICY_NAME" --policy-document file:///tmp/aws-lbc-iam-policy.json
        else
          echo "AWS Load Balancer Controller IAM policy already exists; skipping policy creation"
        fi
      - echo "Associating IAM OIDC provider"
      - eksctl utils associate-iam-oidc-provider --cluster "dev-eks" --region "$AWS_DEFAULT_REGION" --approve
      - echo "Creating or updating IRSA service account"
      - eksctl create iamserviceaccount --cluster "dev-eks" --region "$AWS_DEFAULT_REGION" --namespace kube-system --name aws-load-balancer-controller --role-name "dev-eks-aws-load-balancer-controller" --attach-policy-arn "$POLICY_ARN" --override-existing-serviceaccounts --approve
      - echo "Installing AWS Load Balancer Controller"
      - helm repo add eks https://aws.github.io/eks-charts
      - helm repo update
      - VPC_ID=$(aws eks describe-cluster --name "dev-eks" --region "$AWS_DEFAULT_REGION" --query 'cluster.resourcesVpcConfig.vpcId' --output text)
      - |
        helm upgrade --install aws-load-balancer-controller eks/aws-load-balancer-controller \
          -n kube-system \
          --wait \
          --timeout 10m \
          --set clusterName="dev-eks" \
          --set enableServiceMutatorWebhook=false \
          --set serviceAccount.create=false \
          --set serviceAccount.name=aws-load-balancer-controller \
          --set region="$AWS_DEFAULT_REGION" \
          --set vpcId="$VPC_ID"
      - echo "Waiting for AWS Load Balancer Controller rollout"
      - kubectl rollout status deployment/aws-load-balancer-controller -n kube-system --timeout=5m
      - |
        for i in $(seq 1 12); do
          if kubectl get mutatingwebhookconfiguration aws-load-balancer-webhook -o jsonpath='{.webhooks[0].clientConfig.caBundle}' 2>/dev/null | grep -q .; then
            echo "AWS Load Balancer Controller webhook CA bundle is present"
            break
          fi
          if [ "$i" -eq 12 ]; then
            echo "AWS Load Balancer Controller webhook CA bundle did not become ready in time"
            exit 1
          fi
          echo "Waiting for webhook CA bundle ($i/12)"
          sleep 10
        done
      - echo "Exposing ArgoCD through a public load balancer"
      - |
        for i in $(seq 1 6); do
          if kubectl apply -f ../../manifests/argocd-server-public-service.yaml; then
            kubectl get svc argocd-server-public -n argocd
            break
          fi
          if [ "$i" -eq 6 ]; then
            echo "Failed to create the public ArgoCD service after retries"
            exit 1
          fi
          echo "Retrying ArgoCD public service creation ($i/6)"
          sleep 15
        done
      - echo "Infra bootstrap for dev completed"
